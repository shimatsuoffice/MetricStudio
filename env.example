# === Meta (Facebook) App Credentials ===
META_APP_ID=YOUR_META_APP_ID
META_APP_SECRET=YOUR_META_APP_SECRET

# OAuth Redirect URI (must match Meta App settings)
REDIRECT_URI=https://your-review-domain.example/auth/callback

# Session secret (change to a long random string)
APP_SECRET=please-change-me

# CORS allowlist (comma-separated)
ALLOWED_ORIGINS=https://your-review-domain.example
